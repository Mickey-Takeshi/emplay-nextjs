# リニューアル後デザインレビュー（2026-09-01）

対象: `emplay.jp` 本番（コミット `3670cba` / push済み・本番反映済み）
方法: 12ページ × 3画面幅（1440 / 768 / 390px）を実ブラウザで機械計測 + 目視

---

## 総評

**構造は健全。** 12ページ×3画面幅の36通りで、**横スクロール・画像読み込み失敗・404・コンソールエラーはゼロ**。
alt属性の欠落なし、画像の歪みなし。リニューアルの実装品質は高い。

改善余地は「①公開中の価格誤り」「②画像の縦横比」「③一部の文字サイズ」に集中している。

---

## P0: 事業影響あり・即対応

### 1. HP制作の価格が 30万円 で公開されている（正は45万円）

7月の価格改定（相場の約70%＝45〜150万円）が、リニューアル実装で30万円に戻り**本番反映済み**。

| 掲載箇所 | 現在の表示 |
|---|---|
| トップ サービスカード | 初期費用**30万円**から制作します |
| サービス一覧 HP制作 | 初期費用**30万円**から |
| HP制作LP ライトプラン | **¥300,000〜** |
| HP制作LP 制作費の目安 | **30〜150万円** |
| HP制作LP 比較表 | ○ 制作**30万円**〜 |
| HP制作LP 導線文 | サイト制作からなら初期費用**30万円**〜 |

**注意**: 同じ「30万円」でも次の2つは**正しい値**なので変更しないこと。
- SEO/AIO月額プラン グロース `¥300,000〜`（20/30/45万の体系）
- 比較表「一般的なSEO会社 月30万円〜」（相場を示す数字）

**再発の原因**: 設計ドキュメント3本に「HP制作の初期費用は30万円からとする」と明記されている
（`docs/design-renewal/` の competitor-research / concept-a-visual-variants / site-wide-design-system）。
**ドキュメントを直さないと次のリニューアル作業で再び書き戻る。**

---

## P1: 表示品質

### 2. 画像の縦横比が合わず、最大43%が余白になっている

renewal画像は**全12枚が 1536×1024（3:2）で統一**されているが、配置先の枠は **1.03〜2.65** と幅広い。
`object-fit: contain` のため、差分がそのまま余白になり、**イラストが小さく浮いて見え、カード間で大きさが揃わない**。

| ページ | スロット | 枠(PC) | 枠の比 | 余白率 |
|---|---|---|---|---|
| /service | サービス5行 | 557×210 | 2.65 | **43%** |
| トップ | ACADEMYカード | 429×418 | 1.03 | **32%** |
| トップ | HP制作カード（小） | 449×208 | 2.16 | **31%** |
| トップ | 広告/CRMカード | 429×378 | 1.13 | **24%** |
| /service | プロセス図 | 750×655 | 1.15 | **24%** |
| トップ | CRMカード（小） | 414×208 | 1.99 | 25% |
| トップ | HP制作カード（大） | 762×418 | 1.82 | 18% |
| トップ | エコシステム図 | 664×520 | 1.28 | 15% |

対応は2通り。**併用を推奨**する。
- **A. 画像を枠に合わせて作り直す** → [画像生成仕様書](image-generation-spec-2026-09-01.md)
- **B. 枠を画像に合わせる**（CSSで `aspect-ratio: 3/2` を与える）。生成コストゼロだがレイアウトの意図が変わる

### 3. パンくずの文字サイズがPCとSPで逆転している

実測: **PC 11px / SP 12px**。大画面のほうが小さいという不自然な状態。
原因はトークンの当て方（ベースに `--text-2xs`、モバイルで `--text-xs` を上書き）。
**両方 12px（`--text-xs`）に統一**するのが正しい。

### 4. /service の価格ラベルが11px

`初期費用30万円から` `料金は要問い合わせ` `研修プログラムを見る ↗` が **11px**。
11px は英語ラベル専用のサイズで、**日本語の最小は12px**（デザインシステム §3 のルール）。
価格は判断に直結する情報なので **13px（`--text-sm`）** が妥当。

### 5. モバイル用の軽量画像がない

旧画像には `-sm` 派生が **12枚**あったが、renewal画像は **0枚**。
390px幅の端末にも **1536px / 1774px の原寸**を配信している。
`srcset` で幅960px程度の派生を出し分けたい（合計1.1MB → 半分以下になる）。

---

## P2: 軽微

### 6. /service/creative のSPで見出しが3pxはみ出す
`ランディングページ（LP）` が `scrollWidth 207 > clientWidth 204`。`overflow: visible` のため
切れずに少しはみ出すだけだが、括弧付き見出しが枠に収まっていない。折り返し許可か17px→16pxで解消。

### 7. タップ領域44px未満が21〜36個/ページ
全ページで一定数ある（ブログSPが最多36）。`--tap-min: 44px` は定義済みなので、
ヘッダーナビ・フッターリンク・カード内リンクに padding を足して当てる。

---

## 問題がなかった項目（確認済み）

- 横スクロール: 12ページ × 3画面幅すべてで **0px**
- 画像: 13スロット全て読み込み成功。歪み・alt欠落なし
- HTTPエラー・JSエラー: なし
- 日本語の負のletter-spacing: なし
- 文字の切れ（clip）: 実害のあるものは §6 の1件のみ

---

## 対応記録（2026-09-01 追記）

### 追加で見つかった不具合: トップのサービスカードが重なっていた（P0）

レビュー時には検出できていなかった。`.renewal-service-grid` が `grid-auto-rows:380px` の固定行に対し、
1枚目と5枚目だけ `min-height:420px` を持っていたため、**行から40pxはみ出して次の行のカードと重なっていた。**

実測（1440px）:

| | 重なり | 行内の下端ズレ |
|---|---|---|
| カード01 × カード03/04 | 縦26px | — |
| 行1（01 vs 02） | — | 40px |
| 行2（03/04 vs 05） | — | 40px |

結果、01の説明文「…初期費用45万円から制作します。」の下端が隠れ、05だけ下に飛び出していた。

### 実施した修正

| # | 対象 | 修正内容 |
|---|---|---|
| 1 | トップ サービスカード | 12カラム＋固定行高 → 6カラム＋画像比で自動高さ。キャプションは画像直下に固定 |
| 2 | トップ ビジュアルレール | 不均等カラム＋高さ210px固定 → 等幅4カラム＋`aspect-ratio:3/2` |
| 3 | トップ Our Role | `height:520px` → `aspect-ratio:1200/1040` |
| 4 | トップ process / academy | `min-height` を `clamp()` 化し、1200px以下で縦積み |
| 5 | /service サービス5行 | `height:210px` → `aspect-ratio:13/5` |
| 6 | /service プロセス図 | 1200px以下で縦積み＋`aspect-ratio:1200/1040` |
| 7 | 両ページ | 1000pxのタブレット帯を追加（従来は700pxまで desktop レイアウトのままだった） |

### 検証（実ブラウザ実測 / dev サーバ）

対象幅: 390 / 700 / 701 / 768 / 900 / 1000 / 1001 / 1024 / 1100 / 1200 / 1201 / 1280 / 1320 / 1440 / 1600

| 指標 | 修正前 | 修正後 |
|---|---|---|
| 要素の重なり | 2件（全デスクトップ幅） | **0件** |
| 行内の下端ズレ | 40px × 2行 | **0px** |
| 画像の余白率（最大） | 64%（768px時） | **14%**（process図、1201〜1300pxのみ） |
| 横スクロール | 0px | 0px |
| 文字の切れ | 1件（カード01の説明文） | **0件** |

全ページ（トップ / service / service配下4 / company / careers / contact / blog / news / 記事詳細 / privacy / site-map）で
重なり・横スクロール・文字切れなし。`npm run build` 成功（293ページ生成）。

### 残っている軽微な項目

- `.service-process` / `.renewal-process` の図版は、幅1200〜1350pxでテキスト列の方が高くなるため余白が14〜26%残る。
  左右分割の構造上避けられず、実害はない（罫線・整列のズレではない）。
- サイト全体のブレークポイントが 540/700/720/768/840/900/960/1000/1024/1200 と混在している。
  今回はトップと/serviceのみ整理した。他ページは表示上の不具合がないため未着手。
